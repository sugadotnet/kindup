@using APPBASE.Helpers
@using APPBASE.Svcapp


<!-- sidebar menu: : style can be found in sidebar.less -->
<ul class="sidebar-menu">
    <li class="active">
        <a href="@Url.Action("Index", "Home")" class="bg-orange">
            <i class="fa fa-home"></i> <span>Home</span>
        </a>
    </li>
    
    @if (hlpConfig.SessionInfo.getAppUsername().ToUpper() == valDFLT.SYSADMIN_USER) { Html.RenderPartial("_PartialMenuSYSADMIN"); }
    else {
        // 1 = SM	SCHOOL MASTER
        // 2 = SA	SCHOOL ACCOUNTANT
        // 3 = PC	PROGRAM COORDINATOR
        // 4 = SC	SCHOOL COORDINATOR
        // 5 = CT	CLASS TEACHER
        // 6 = ADM	ADMINISTRATION
        // 7 = P	PARENT
        Html.RenderPartial("_PartialMenu" + hlpConfig.SessionInfo.getAppRoleId().ToString());
    }

</ul>
