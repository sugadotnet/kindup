@model APPBASE.Models.StudentVM
@using APPBASE.Helpers
@using APPBASE.Svcbiz

@{string vsErrClass = "";}
@{string vsDisableButton = "";}
@{Boolean isShowDropdown = true;}
@{string vsReadonlyTextboxKey = "Readonly";}
@{string vsReadonlyTextbox = "";}
@if (ViewBag.CRUD_type == hlpFlags_CRUDOption.CREATE) { vsReadonlyTextboxKey = ""; }
@if ((ViewBag.CRUD_type == hlpFlags_CRUDOption.VIEW) || (ViewBag.CRUD_type == hlpFlags_CRUDOption.DELETE)) { vsDisableButton = "disabled"; vsReadonlyTextbox = "Readonly"; isShowDropdown = false; }
@{string vsDateFormat = "'alias': 'dd/mm/yyyy'";}

<div class="row">
    <div class="col-xs-12">
        @if (ViewData.ModelState["BRANCH_ID0"] != null) { vsErrClass = "has-error"; } else { vsErrClass = ""; }
        <div class="form-group @vsErrClass">
            <label for="BRANCH_ID">Cabang/Pusat</label>
                <select id="BRANCH_ID" name="DETAIL.BRANCH_ID" class="form-control select2" data-placeholder="Select Cabng/Pusat" style="width: 100%;" @vsReadonlyTextbox>
                    <option selected value=""></option>
                    @if (ViewBag.BRANCHALL != null)
                    {
                        foreach (var item in ViewBag.BRANCHALL)
                        {

                            if (Model.DETAIL.BRANCH_ID == item.ID)
                            {<option selected value="@item.ID">@item.BRANCH_DESC</option> }
                            else
                            {<option value="@item.ID">@item.BRANCH_DESC</option> }
                        }
                    }
                </select>

            @if (ViewData.ModelState["BRANCH_ID1"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("BRANCH_ID1")
                    </label>
            }
            @if (ViewData.ModelState["BRANCH_ID2"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("BRANCH_ID2")
                    </label>
            }
        </div> <!-- /.form-group -->
    </div> <!-- /.col-xs-12 -->
</div><!-- /.row -->

<div class="row">
    <div class="col-xs-12">
        @if (ViewData.ModelState["REGNO0"] != null) { vsErrClass = "has-error"; } else { vsErrClass = ""; }
        <div class="form-group @vsErrClass">
            <label for="REGNO">Nomor Formulir Pendaftaran</label>
            <input type="text" id="REGNO" name="DETAIL.REGNO" value="@Html.DisplayFor(model => model.DETAIL.REGNO)" class="form-control" maxlength = "20" @vsReadonlyTextbox />

            @if (ViewData.ModelState["REGNO1"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("REGNO1")
                    </label>
            }
            @if (ViewData.ModelState["REGNO2"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("REGNO2")
                    </label>
            }
        </div> <!-- /.form-group -->
    </div> <!-- /.col-xs-12 -->
</div><!-- /.row -->

<div class="row">
    <div class="col-xs-12">
        @if (ViewData.ModelState["YEAR_ID0"] != null) { vsErrClass = "has-error"; } else { vsErrClass = ""; }
        <div class="form-group @vsErrClass">
            <label for="YEAR_ID">Tahun Ajaran</label>
                <select id="YEAR_ID" name="DETAIL.YEAR_ID" class="form-control select2" data-placeholder="Select Tahun Ajaran" style="width: 100%;" @vsReadonlyTextbox>
                    <option selected value=""></option>
                    @if (ViewBag.YEAR != null)
                    {
                        foreach (var item in ViewBag.YEAR)
                        {

                            if (Model.DETAIL.YEAR_ID == item.ID)
                            {<option selected value="@item.ID">@item.YEAR_DESC</option> }
                            else
                            {<option value="@item.ID">@item.YEAR_DESC</option> }
                        }
                    }
                </select>

            @if (ViewData.ModelState["YEAR_ID1"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("YEAR_ID1")
                    </label>
            }
            @if (ViewData.ModelState["YEAR_ID2"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("YEAR_ID2")
                    </label>
            }
        </div> <!-- /.form-group -->
    </div> <!-- /.col-xs-12 -->
</div><!-- /.row -->

<div class="row">
    <div class="col-xs-12">
        @if (ViewData.ModelState["SEMESTER_ID0"] != null) { vsErrClass = "has-error"; } else { vsErrClass = ""; }
        <div class="form-group @vsErrClass">
            <label for="SEMESTER_ID">Semester</label>
                <select id="SEMESTER_ID" name="DETAIL.SEMESTER_ID" class="form-control select2" data-placeholder="Select Semester" style="width: 100%;" @vsReadonlyTextbox>
                    <option selected value=""></option>
                    @if (ViewBag.SEMESTER != null)
                    {
                        foreach (var item in ViewBag.SEMESTER)
                        {

                            if (Model.DETAIL.SEMESTER_ID == item.ID)
                            {<option selected value="@item.ID">@item.SEMESTER_DESC</option> }
                            else
                            {<option value="@item.ID">@item.SEMESTER_DESC</option> }
                        }
                    }
                </select>

            @if (ViewData.ModelState["SEMESTER_ID1"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("SEMESTER_ID1")
                    </label>
            }
            @if (ViewData.ModelState["SEMESTER_ID2"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("SEMESTER_ID2")
                    </label>
            }
        </div> <!-- /.form-group -->
    </div> <!-- /.col-xs-12 -->
</div><!-- /.row -->

<div class="row">
    <div class="col-xs-12">
        @if (ViewData.ModelState["CLASSTYPE_ID0"] != null) { vsErrClass = "has-error"; } else { vsErrClass = ""; }
        <div class="form-group @vsErrClass">
            <label for="CLASSTYPE_ID">Kelas yang diikuti</label>
                <select id="CLASSTYPE_ID" name="DETAIL.CLASSTYPE_ID" class="form-control select2" data-placeholder="Select Kelas yang diikuti" style="width: 100%;" @vsReadonlyTextbox>
                    <option selected value=""></option>
                    @if (ViewBag.CLASSTYPE != null)
                    {
                        foreach (var item in ViewBag.CLASSTYPE)
                        {

                            if (Model.DETAIL.CLASSTYPE_ID == item.ID)
                            {<option selected value="@item.ID">@item.CLASSTYPE_NAME</option> }
                            else
                            {<option value="@item.ID">@item.CLASSTYPE_NAME</option> }
                        }
                    }
                </select>

            @if (ViewData.ModelState["CLASSTYPE_ID1"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("CLASSTYPE_ID1")
                    </label>
            }
            @if (ViewData.ModelState["CLASSTYPE_ID2"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("CLASSTYPE_ID2")
                    </label>
            }
        </div> <!-- /.form-group -->
    </div> <!-- /.col-xs-12 -->
</div><!-- /.row -->

<div class="row">
    <div class="col-xs-12">
        @if (ViewData.ModelState["CLASSROOM_ID0"] != null) { vsErrClass = "has-error"; } else { vsErrClass = ""; }
        <div class="form-group @vsErrClass">
            <label for="CLASSROOM_ID">Kelompok</label>
                <select id="CLASSROOM_ID" name="DETAIL.CLASSROOM_ID" class="form-control select2" data-placeholder="Select Kelompok" style="width: 100%;" @vsReadonlyTextbox>
                    <option selected value=""></option>
                    @foreach (var item in ViewBag.CLASSROOM)
                    {

                        if (Model.DETAIL.CLASSROOM_ID == item.ID) {<option selected value="@item.ID">@item.CLASSROOM_NAME</option> }
                        else {<option value="@item.ID">@item.CLASSROOM_NAME</option> }
                    }
                </select>

            @if (ViewData.ModelState["CLASSROOM_ID1"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("CLASSROOM_ID1")
                    </label>
            }
            @if (ViewData.ModelState["CLASSROOM_ID2"] != null)
            {
                    <label class="control-label" for="inputError">
                    <i class="fa fa-times-circle-o"></i> @Html.ValidationMessage("CLASSROOM_ID2")
                    </label>
            }
        </div> <!-- /.form-group -->
    </div> <!-- /.col-xs-12 -->
</div><!-- /.row -->
